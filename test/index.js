var expect = require('expect.js');
var morphemes = require('../lib');

describe('hangul-morphemes', function () {
    it('should have unit test!', function () {
        expect('we expected this package author to add actual unit tests.').to.be.not.true;
    });
});
